#lang beagle/js
(ns wake.fixtures.wrong-derived-expression
  (:require [wake.core :as wake]))

(wake/defentity release Release
  [id: String
   title: String
   label: String]
  :id
  {:title :create}
  [(wake/derived-field :label "title")])
